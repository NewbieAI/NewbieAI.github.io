"use strict";

// file created on May 10th,
// this game will also be rendered in html5 canvas,
// and it will feature a game where user has to 
// balance a pole on the cart

class Cartpole {
    static GRAVITY = -1.0;
    static CART_MASS = 5.0;
    static POLE_MASS = 1.0;

    constructor(canvasContext) {
        // create an instance of the cartpole game
        reset();
    }

    reset() {
        // reset the state of the game
    }

    evolve() {
        // evolve the state of the system according to the 
        // equations of motion.
    }
}
